# EazeNote Backend – Memory-Optimised Dependencies
# No torch, no sentence-transformers, no heavy ML libs.
# Stays well under Render free tier 512 MB limit.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Vector DB (pure Python + C extensions, ~50 MB)
chromadb==0.5.23

# PDF parsing (no ML, just C bindings)
pymupdf==1.24.14

# HTTP + env
requests==2.32.3
python-dotenv==1.0.1

# File uploads (required by FastAPI for multipart/form-data)
python-multipart==0.0.17
